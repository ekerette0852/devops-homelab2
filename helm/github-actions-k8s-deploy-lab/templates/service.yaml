apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-actions-k8s-deploy-lab.fullname" . }}
  labels:
    {{- include "github-actions-k8s-deploy-lab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github-actions-k8s-deploy-lab.selectorLabels" . | nindent 4 }}
