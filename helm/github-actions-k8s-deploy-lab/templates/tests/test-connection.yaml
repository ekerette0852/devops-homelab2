apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github-actions-k8s-deploy-lab.fullname" . }}-test-connection"
  labels:
    {{- include "github-actions-k8s-deploy-lab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github-actions-k8s-deploy-lab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
